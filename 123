@Test
void testCompare_EqualBranch() {
    String mockString = Mockito.mock(String.class);
    when(mockString.compareTo(any())).thenReturn(0);

    LoansbbQuotationItemInfo o1 = Mockito.mock(LoansbbQuotationItemInfo.class);
    LoansbbQuotationItemInfo o2 = Mockito.mock(LoansbbQuotationItemInfo.class);

    when(o1.getFeaturecd()).thenReturn(mockString);
    when(o2.getFeaturecd()).thenReturn("anything");

    assertEquals(0, sortingCoverage.compare(o1, o2));
}