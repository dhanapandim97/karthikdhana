@Test
void testInterruptedExceptionInSleep() {
    // Arrange
    MyClass obj = new MyClass(); // your class under test

    // Interrupt the test thread BEFORE calling the method
    Thread.currentThread().interrupt();

    // Act
    obj.yourMethodWithSleep();

    // ✅ After calling the method, verify the interrupt block was executed
    // If your code only logs, check that the interrupt flag is no longer set
    assertFalse(Thread.currentThread().isInterrupted(), "Interrupt should have been handled");
}