@Test
void testEnglishBranch_FrAfterEn() {
    PropertiesFileReader reader = createReaderWith("R1", "SomeText[EN]Hello[FR]Bonjour");
    String result = reader.getKeyValueItem("R1", ErrorDisplayLanguage.ENGLISH);
    assertTrue(result.contains("Hello"), "Should extract English text when FR comes after EN");
}

@Test
void testFinalReturnPrefixPlusValue_DefaultCase() {
    PropertiesFileReader reader = createReaderWith("R2", "NoFlagsHere");

    // Mock a fake language that isn’t ENGLISH or FRENCH
    ErrorDisplayLanguage fakeLang = mock(ErrorDisplayLanguage.class);
    when(fakeLang.getLangStr()).thenReturn("XX");

    String result = reader.getKeyValueItem("R2", fakeLang);

    // This should come from the very last "return prefix + value;"
    assertEquals("R2:NoFlagsHere", result);
}